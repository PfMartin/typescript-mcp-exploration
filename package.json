{
  "name": "typescript-mcp-server",
  "version": "1.0.0",
  "main": "server.ts",
  "author": "PfMartin",
  "keywords": [
    "mcp",
    "typescript"
  ],
  "license": "UNLICENSED",
  "scripts": {
    "build": "npx tsc",
    "build:watch": "npx tsc --watch",
    "server:start": "node dist/server.js",
    "server:inspect": "set DANGEROUSLY_OMIT_AUHT=true && npx @modelcontextprotocol/inspector npm run server:start"
  },
  "devDependencies": {
    "@modelcontextprotocol/inspector": "^0.16.2",
    "@types/node": "^24.2.1",
    "typescript": "^5.9.2"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.17.2"
  }
}
